  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }C  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }M  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }@  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }/  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }C  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }j  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }O  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }@  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }/  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }A  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }G  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }R  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }M  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }@  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }/  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }M  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }A  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }A  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }L  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }O  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }G  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }A  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }A  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }M  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }M  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }M  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }R  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }@  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }/  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }/  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }2  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }U  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }/  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }/  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }/  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }H  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }U  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }H  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }U  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }U  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }H  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }H  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }O  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }/  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }/  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }M  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }/  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }@  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }C  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }C  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }M  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }R  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }M  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }M  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }M  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }U  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }/  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }U  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }/  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }x  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }E  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }H  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }C  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }O  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }H  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }U  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }|  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }L  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }G  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }O  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }H  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }O  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }E  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }|  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }Y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }Y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }U  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }R  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }M  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }R  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }<  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }E  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }H  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }C  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }@  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }j  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }M  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }A  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }A  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }L  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }O  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }G  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }A  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }A  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }k  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }O  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }G  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }G  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }G  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }A  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }A  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }L  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }E  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }H  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }U  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }x  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }O  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }L  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }L  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }E  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }U  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }E  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }x  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }U  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }C  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }#  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }7  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }5  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }1  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }9  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }R  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }H  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }U  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }G  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }k  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }k  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }H  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }Y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }Y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }Y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }Y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }E  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }G  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }&  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }&  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }O  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }O  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }G  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }Y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }H  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }A  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }x  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }A  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }C  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }#  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }7  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }5  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }1  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }9  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }x  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }G  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }x  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }x  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }Y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }H  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }x  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }x  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }G  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }A  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }x  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }G  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }H  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }G  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }q  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }Y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }q  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }q  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }Y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }E  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }E  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }C  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }R  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }N  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }R  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }R  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }N  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }R  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }N  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }E  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }N  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }|  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }|  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }!  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }O  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }x  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }x  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }|  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }H  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }O  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }!  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }x  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }x  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }!  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }!  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }O  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }!  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }A  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }M  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }k  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }A  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }A  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }x  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }C  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }#  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }7  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }5  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }1  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }9  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }H  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }U  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }x  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }U  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }x  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }E  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }C  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }#  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }7  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }5  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }1  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }9  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }E  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }x  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }C  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }B  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }C  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }#  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }7  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }5  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }1  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }9  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }R  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }H  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }U  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }U  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }R  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }G  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }R  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }R  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }<  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }k  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }k  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }O  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }j  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }R  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }E  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }k  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }|  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }|  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }|  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }|  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }k  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }k  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }N  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }k  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }N  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }k  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }k  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }k  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }k  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }k  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }R  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }V  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }N  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }Y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }N  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }N  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }w  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }H  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }U  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }U  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }O  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }j  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }k  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }>  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }_  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }b  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }|  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }!  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }/  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }\  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }1  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }2  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }\  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }2  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }\  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }2  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }\  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }*  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }A  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }M  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }|  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }M  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }|  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }|  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }/  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }!  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }1  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }1  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }2  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }4  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }U  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }C  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }M  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }&  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }&  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }<  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }1  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }2  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }+  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }1  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }2  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }A  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }M  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }&  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }&  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }1  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }2  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }`  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }$  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }2  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }$  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }`  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }P  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }|  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }|  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }!  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }T  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }D  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }F  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }I  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }?  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }|  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }:  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }!  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }v  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }/  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }^  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }\  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }4  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }\  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }2  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }\  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }2  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }$  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }/  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }z  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }/  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }\  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }/  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }/  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }z  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }3  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }f  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }g  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }4  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }`  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }$  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }$  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }2  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }$  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }2  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }`  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }c  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }[  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }]  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }=  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }s  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }`  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }$  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }4  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }$  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }o  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }h  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }2  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }-  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }$  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }{  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }y  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }.  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }p  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }S  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }a  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }(  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }2  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  },  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }0  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }'  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  })  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }`  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }t  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }r  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }i  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }m  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }e  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }d  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }|  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }|  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }n  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }u  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }l  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  };  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }   private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }}  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
  private formatDateForPayload(value?: string | null): string | null {\n    if (!value) {\n      return null;\n    }\n\n    const formatted = this.formatDateForInput(value);\n    return /^\\d{4}-\\d{2}-\\d{2}$/.test(formatted) ? formatted : null;\n  }
